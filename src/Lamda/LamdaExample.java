package Lamda;

public class LamdaExample {
//  자바에서 람다식은 익명 객체
//  자바는 함수만 가질수없기에 익명객체를 통해서 구현해야한다.

    public static void main(String[] args) {
//
//        MyFunction myFunction = (a, b) -> a + b;
//        System.out.println(myFunction.max(3, 5));
//
//        int value = myFunction.max(3,4);
//        System.out.println(value);
//
////     정의된 인터페이스는 a b만을 받기에 변환 사용가능
//       MyFunction myFunction2 = (a,b) -> a > b ? a:b;
//        System.out.println(myFunction2.max(3,52));

//     클래스 내부 함수형 인터페이스 사용
//       Minus myFunction3 = (a, b) -> a < b ? 1 : 2;
//        System.out.println(myFunction3.minus(3,5));
    }




//  클래스 내부 인터페이스 정의
//    interface Minus {
//        public abstract int minus(int a,int b);
//    }
}
